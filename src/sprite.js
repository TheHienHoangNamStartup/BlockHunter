class Sprite {
  constructor(row, color) {
    this.row = row;
    this.color = color;
    this.visible = true;
    this.scale = 1;
  }



  setAnimation() {

  }

  getVisible() {
    return this.visible;
  }

  setVelocity(x, y) {

  }

  getScale() {
    return this.visible;
  }

  setScale(scale) {
    this.visible = scale;
  }

  // Checks if the sprite is touching the target sprite
  isTouching(target) {

  }

  destroy() {

  }
}
